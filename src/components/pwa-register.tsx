"use client";

import { useEffect } from "react";

export function PWARegister() {
    // Disabled for Desktop/Tauri to prevent Cache/addAll errors
    // The desktop app handles its own offline capabilities.
    return null;
}
